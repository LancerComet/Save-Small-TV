/**
 * Define type for bitmap.
 */
type TBitmap = string[]

/**
 * Define type for bitmap.
 */
type TBitmaps = TBitmap[]

/**
 * Define type for color map.
 */
type TColorMap = {[bitmap: string]: string}

/**
 * Define type for texture.
 */
type TTextures = Array<ImageData>

/**
 * Define type for direction.
 */
type TDirection = 'L' | 'R' | 'T' | 'B'
