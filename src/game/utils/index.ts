/**
 * Math floor.
 *
 * @param {number} num
 * @returns
 */
function floor (num: number) {
  return Math.floor(num)
}

/**
 * Generate random number.
 *
 * @returns
 */
function rand () {
  return Math.random()
}

export {
  floor,
  rand
}
